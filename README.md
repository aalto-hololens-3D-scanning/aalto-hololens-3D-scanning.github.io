# Installation

```Bash
xcode-select --install    # Mac only

curl -sSL https://get.rvm.io | bash -s stable

rvm install 2.3
rvm use 2.3
gem install bundle
gem install jekyll
gem install nokogiri

bundle install
jekyll serve
```
